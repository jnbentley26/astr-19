import matplotlib.pyplot as plt
import numpy as py
%matplotlib inline
x = np.arange(0,1,0.01)
sinx = np.sin(x)
cosx = np.cos(x)
fig, (ax1, ax2) = plt.subplots(1, 2)
ax1.set_title("Sin(x) vs x")
ax1.plot(sinx, x)
ax2.set_title("Cos(x) vs x")
ax2.plot(cosx, x)
